# sim-lib-compute-auto

Automatic compute-site selection for SIM tensors.

`AutoTensorExecutor` uses a compatible modeled profile when one is supplied and
otherwise delegates to the published CPU tensor executor from `sim-numbers`.

