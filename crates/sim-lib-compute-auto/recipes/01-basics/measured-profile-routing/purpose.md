Shows automatic compute placement using a supplied Table-backed measured profile.
The route accepts device placement only when bounded measurement evidence is
fresh, compatible, and conclusive; otherwise it records the CPU fallback.
