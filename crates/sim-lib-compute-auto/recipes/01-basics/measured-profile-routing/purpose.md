Shows automatic compute placement using a supplied Table-backed profile. The
current fixture is synthetic and demonstrates CPU fallback rather than physical
device acceptance. The route accepts device placement only when bounded evidence
is fresh, compatible, conclusive, physical-device, and not caller-renamed;
otherwise it records the CPU fallback.
