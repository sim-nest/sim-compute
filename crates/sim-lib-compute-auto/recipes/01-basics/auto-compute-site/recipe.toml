id = "auto-compute-site"
title = "Auto compute site"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
expected = "expected.txt"
order = 10
tags = ["compute", "tensor", "auto", "fallback"]
requires = ["compute/model", "compute/auto", "numbers/tensor"]

[[expect]]
form = 0
result = "(compute auto-site (site site/compute/auto) (without-compatible-profile cpu))"
