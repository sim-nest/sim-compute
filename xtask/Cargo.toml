[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
description = "Repository automation wrapper for generated documentation and policy checks."
readme = "README.md"
publish = false

[dependencies]
sim-kernel = { version = "0.2.0" }
sim-lib-compute-cli = { path = "../crates/sim-lib-compute-cli" }
toml_edit = "0.25.13"

[lints]
workspace = true
